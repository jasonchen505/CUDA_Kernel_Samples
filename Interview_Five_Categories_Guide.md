# 技术面试五类问题应对指南 — LLM 算法实习

> 基于 CUDA_Kernel_Samples 项目知识 + LLM 算法面试场景
> 核心目标：展示"做过项目"的深度，而非"背过概念"的广度

---

## 总览：五类问题的本质

| 类别 | 面试官在考察什么 | 你的回答策略 |
|------|-----------------|-------------|
| 底层原理 | 你是否理解"为什么"而非只知道"是什么" | 问题→方案→局限→改进 |
| 实验验证 | 你是否真正动手做过，而非纸上谈兵 | 实验设计→指标选择→结果分析 |
| 问题定位 | 遇到问题时你的排查思路是否清晰 | 现象→假设→验证→定位→修复 |
| 工程落地 | 理论能否变成实际可用的系统 | 瓶颈分析→方案选择→部署细节 |
| 业务理解 | 你是否知道什么值得做，什么不值得做 | 场景→成本→收益→优先级 |

---

## 一、底层原理深入理解

### 1.1 面试官的考察逻辑

```
"这个方法解决什么问题？"
    → 考察你是否理解动机，而非死记硬背
    
"存在哪些局限性？"
    → 考察你是否做过对比实验，遇到过失败
    
"有哪些改进方法？"
    → 考察你是否有系统性思考，而非只知一种方案
```

### 1.2 回答框架：问题→方案→局限→改进

**万能结构**：
1. **问题**：这个方法解决什么问题？之前的方案有什么痛点？
2. **方案**：核心思想是什么？为什么有效？
3. **局限**：什么场景下会失效？有什么trade-off？
4. **改进**：有哪些变体或替代方案？

### 1.3 具体问题示例

#### Q1: FlashAttention 解决了什么问题？

**回答**：

> **问题**：标准 Attention 需要存储 N×N 的 attention matrix，对于序列长度 4096，这个矩阵是 4096² × 2 bytes ≈ 32MB（FP16）。在训练长序列时，显存占用成为瓶颈。
>
> **方案**：借鉴 SGEMM 优化中的 tiling 思想（我在项目中实现过），将 Q/K/V 分成小块，每块放入 SRAM（类似 GPU 的 shared memory），在 SRAM 中完成 softmax 和乘法，避免将完整 attention matrix 写回 HBM。
>
> **局限**：
> - 需要 online softmax（两遍扫描），实现复杂
> - 对硬件有要求，需要足够的 SRAM
> - 不是所有 attention 变体都能直接用（如 sparse attention）
>
> **改进**：
> - FlashAttention-2：优化了并行度和工作分区
> - FlashAttention-3：利用 Hopper 架构的异步特性
> - Ring Attention：用于超长序列的分布式 attention

**项目关联**：
```cpp
// SGEMM 的 tiling 策略（sgemm/kernel2.cu）
// 将 A 和 B 分成 BM×BK 和 BK×BN 的小块，放入 shared memory
// FlashAttention 对 Q/K/V 做完全相同的事情
__shared__ float As[BM * BK];
__shared__ float Bs[BK * BN];
```

#### Q2: LoRA 为什么高效？有什么局限？

**回答**：

> **问题**：全量微调 7B 模型需要存储 14GB 参数 + 优化器状态，显存和计算开销大。
>
> **方案**：低秩分解 W = W₀ + BA，其中 B∈R^(d×r), A∈R^(r×k)，r << min(d,k)。只训练 A 和 B，参数量从 d×k 降到 r×(d+k)。
>
> **局限**：
> - 秩 r 的选择需要实验，太小会欠拟合，太大失去效率优势
> - 对所有层用同一个 r 可能不是最优的
> - 不适合任务差异很大的多任务学习
>
> **改进**：
> - AdaLoRA：自适应调整每层的秩
> - DoRA：分解权重为幅度和方向
> - QLoRA：量化基础模型 + LoRA，进一步减少显存

**项目关联**：
```
LoRA 的低秩矩阵 A 和 B 很小，可以放在更快的内存层级
类似项目中 float4 向量化访存的思路：减少访存量 = 提高效率
```

#### Q3: 量化方法（GPTQ vs AWQ vs GGUF）的区别？

**回答**：

> **问题**：FP16 的 7B 模型需要 14GB 显存，推理时还需要 KV cache，单卡装不下。
>
> **方案对比**：
>
> | 方法 | 量化粒度 | 校准数据 | 特点 |
> |------|---------|---------|------|
> | GPTQ | 逐层量化 | 需要 | 基于 OBQ，逐列量化，误差最小化 |
> | AWQ | 逐通道量化 | 需要 | 保护重要权重（激活值大的通道） |
> | GGUF | 逐块量化 | 不需要 | CPU/GPU 混合推理，适合本地部署 |
>
> **局限**：
> - INT4 量化对 outlier 敏感，可能损失精度
> - 不同任务对量化的敏感度不同
> - 量化后无法再做 LoRA 微调
>
> **改进**：
> - SmoothQuant：平滑激活值分布，减少 outlier 影响
> - FP8：新的量化格式，精度损失更小

**项目关联**：
```cpp
// 项目中的 float4 向量化访存（elementwise/add.cu）
#define FLOAT4(value) *(float4*)(&(value))

// 量化的核心思想类似：用更少的位数表示数据
// INT8 比 FP16 少一半字节 → 访存量减少 → 带宽翻倍
// Tensor Core 的 INT8 吞吐量是 FP16 的 2 倍
```

---

## 二、实验和方案验证能力

### 2.1 面试官的考察逻辑

```
"你怎么做实验的？"
    → 考察你是否有系统性的实验方法论

"指标怎么选的？"
    → 考察你是否理解任务需求，而非盲目跑实验

"结果怎么分析的？"
    → 考察你是否能从数据中得出结论，而非只报告数字
```

### 2.2 回答框架：实验设计→指标选择→结果分析→结论

**万能结构**：
1. **实验设计**：baseline 是什么？变量是什么？数据集是什么？
2. **指标选择**：为什么选这个指标？它反映什么？
3. **结果分析**：结果符合预期吗？不符合的原因是什么？
4. **结论**：这个方案值得上线吗？有什么 trade-off？

### 2.3 具体问题示例

#### Q1: 你怎么验证 LoRA 微调的效果？

**回答**：

> **实验设计**：
> - Baseline：全量微调
> - 对比方案：LoRA (r=8, 16, 32, 64)
> - 数据集：下游任务的 train/dev/test split
> - 控制变量：相同学习率、epoch、数据量
>
> **指标选择**：
> - 主指标：任务相关的 metric（如 accuracy、F1）
> - 辅助指标：训练时间、显存占用、参数量
>
> **结果分析**：
> - r=16 时 accuracy 达到全量微调的 98%，但参数量只有 0.1%
> - r=32 时 accuracy 提升 0.5%，但训练时间增加 30%
> - 结论：r=16 是性价比最高的选择
>
> **追问准备**：
> - "为什么不用更大的 r？" → 边际收益递减，成本增加
> - "怎么选择 LoRA 应用在哪些层？" → 实验对比，通常 attention 层效果最好

**项目关联**：
```
SGEMM 优化中的性能对比方法（sgemm/tools/test.sh）
- 每个 kernel 跑多次，取平均值
- 用 matplotlib 画性能对比图
- 对比 cuBLAS baseline

LoRA 实验也应该：
- 多次运行取平均
- 画学习曲线
- 对比 baseline
```

#### Q2: 你怎么评估一个 RAG 系统的效果？

**回答**：

> **实验设计**：
> - 构建测试集：问题 + 标准答案 + 相关文档
> - 对比方案：
>   - 无 RAG（纯 LLM）
>   - 稠密检索（Dense Retrieval）
>   - 稀疏检索（BM25）
>   - 混合检索（Dense + Sparse）
>
> **指标选择**：
> - 检索指标：Recall@K、MRR、NDCG
> - 生成指标：BLEU、ROUGE、人工评估
> - 端到端指标：准确率、相关性、幻觉率
>
> **结果分析**：
> - 稠密检索在语义相似问题上更好，但对精确匹配不如 BM25
> - 混合检索综合了两者优势
> - 检索质量直接影响生成质量（检索 recall 从 70%→90%，准确率提升 15%）
>
> **追问准备**：
> - "怎么构建高质量测试集？" → 从真实用户 query 采样，人工标注
> - "怎么处理检索结果中的噪声？" → re-ranker、阈值过滤

#### Q3: 项目中的 SGEMM 怎么做性能验证？

**回答**（基于项目实际代码）：

> **实验设计**：
> - Baseline：cuBLAS（NVIDIA 官方优化库）
> - 对比：7 个逐步优化的 kernel 版本
> - 测试矩阵大小：256×256 到 2560×2560
>
> **指标选择**：
> - GFLOPS（每秒浮点运算次数）
> - 相对于 cuBLAS 的百分比
>
> **结果分析**：
> ```
> Kernel 1 (Naive): 1012 GFLOPS → cuBLAS 的 10.8%
> Kernel 2 (Shared Mem): 性能提升 1.5x
> Kernel 3 (Thread Tile): 性能提升 3x
> Kernel 7 (Double Buffer): 9325 GFLOPS → cuBLAS 的 99.7%
> ```
>
> **结论**：
> - Shared memory tiling 是最关键的优化（提升最大）
> - 双缓冲是最后的微调（收益递减但仍有意义）
> - 优化顺序很重要：先做收益大的，再做收益小的

**项目代码**：
```python
# sgemm/tools/plot.py — 自动生成性能对比图
# sgemm/tools/test.sh — 自动化测试流程
# 测试结果保存在 sgemm/test/ 目录下
```

---

## 三、问题定位能力

### 3.1 面试官的考察逻辑

```
"模型上线后能力下降怎么办？"
    → 考察你的排查思路是否系统化

"实验结果和预期不一致怎么办？"
    → 考察你是否能区分 bug 和真实现象
```

### 3.2 回答框架：现象→假设→验证→定位→修复

**万能结构**：
1. **现象**：具体描述问题是什么，量化指标变化
2. **假设**：列出可能的原因，按可能性排序
3. **验证**：怎么排除或确认每个假设
4. **定位**：找到根因
5. **修复**：解决方案是什么，怎么验证修复有效

### 3.3 具体问题示例

#### Q1: 模型上线后 accuracy 突然下降 5%，怎么排查？

**回答**：

> **现象**：线上 A/B 测试，新模型比旧模型 accuracy 下降 5%
>
> **假设**（按可能性排序）：
> 1. **数据分布漂移**：线上数据和训练数据分布不同
> 2. **模型版本错误**：部署了错误的 checkpoint
> 3. **特征工程问题**：线上特征处理和训练不一致
> 4. **量化损失**：INT8 量化导致精度下降
>
> **验证步骤**：
> ```
> Step 1: 检查模型版本
> → 确认 checkpoint hash 和训练日志一致
> → 排除：版本没问题
>
> Step 2: 检查数据分布
> → 对比线上数据和训练数据的统计特征
> → 发现：线上数据中某类 query 占比从 10% 增加到 30%
> → 确认：数据分布漂移
>
> Step 3: 验证假设
> → 用线上数据中的新类别单独测试
> → accuracy 只有 60%，远低于平均值
> → 确认：模型对新类别泛化能力不足
> ```
>
> **修复方案**：
> - 短期：对新类别做 few-shot prompting
> - 长期：收集新类别数据，重新训练
>
> **验证修复**：
> - 在新类别上 accuracy 从 60% 提升到 85%
> - 整体 accuracy 恢复到原有水平

#### Q2: 系统上线后响应变慢，怎么排查？

**回答**：

> **现象**：P99 延迟从 200ms 增加到 2s
>
> **假设**：
> 1. **GPU 显存不足**：KV cache 超出限制，触发 swap
> 2. **请求排队**：并发请求过多，超出处理能力
> 3. **网络瓶颈**：模型服务和前端之间的网络问题
> 4. **硬件故障**：GPU 降频或故障
>
> **验证步骤**：
> ```
> Step 1: 监控指标检查
> → GPU 显存使用率：95%（接近满）
> → GPU 计算利用率：40%（不高）
> → 请求队列长度：50+（正常应该 < 10）
> → 假设1 和 2 都可能是原因
>
> Step 2: 深入分析
> → 检查请求的 sequence length 分布
> → 发现：长序列（>4096 tokens）请求占比从 5% 增加到 20%
> → 长序列占用更多 KV cache，导致：
>   - 单请求显存占用增加
>   - batch size 被迫减小
>   - 吞吐量下降，请求排队
>
> Step 3: 定位根因
> → 上游业务调整，允许用户输入更长文本
> → 模型服务没有相应调整
> ```
>
> **修复方案**：
> - 短期：限制输入长度为 2048 tokens
> - 中期：启用 PagedAttention，优化 KV cache 管理
> - 长期：升级 GPU 显存，或使用模型并行
>
> **验证修复**：
> ```
> 启用 PagedAttention 后：
> - 显存利用率从 95% 降到 70%
> - 请求队列长度从 50 降到 5
> - P99 延迟从 2s 降到 300ms
> ```

#### Q3: CUDA kernel 运行结果和 CPU 参考不一致，怎么排查？

**回答**（基于项目经验）：

> **现象**：softmax kernel 的输出和 CPU 参考实现有 1e-3 的误差
>
> **假设**：
> 1. **数值精度问题**：float32 的浮点误差累积
> 2. **实现 bug**：归约逻辑有错误
> 3. **边界条件**：某些特殊输入（如全 0、NaN）处理不当
>
> **验证步骤**：
> ```
> Step 1: 缩小问题范围
> → 用小规模输入（N=32）测试
> → 误差仍然存在 → 不是边界条件问题
>
> Step 2: 逐步检查
> → 只测 max 归约：误差 < 1e-6
> → 只测 sum 归约：误差 < 1e-6
> → 测完整 softmax：误差 1e-3
> → 问题出在 softmax 的组合逻辑
>
> Step 3: 详细对比
> → 打印每一步的中间结果
> → 发现：sum_kernel 的输入没有减去 max_val
> → Bug：sum_kernel 和 max_kernel 之间的依赖没有正确处理
>
> Step 4: 修复
> → 确保 sum_kernel 读取的是 (input - max_val) 的 exp
> → 重新测试，误差 < 1e-6
> ```

**项目代码**：
```cpp
// reduce/softmax/softmax.cu 中的正确实现
// Step 1: max_kernel 求最大值
max_kernel<<<grid, block>>>(input, max_val, N);

// Step 2: sum_kernel 求和（注意：输入要减去 max_val）
sum_kernel<<<grid, block>>>(input, sum, max_val, N);

// Step 3: softmax_kernel 计算最终结果
softmax_kernel<<<grid, block>>>(input, output, sum, max_val, N);
```

---

## 四、工程落地能力

### 4.1 面试官的考察逻辑

```
"这个方案怎么部署？"
    → 考察你是否知道理论和实践的差距

"上线后怎么保证稳定性？"
    → 考察你是否有生产环境的经验
```

### 4.2 回答框架：瓶颈分析→方案选择→部署细节→监控回滚

**万能结构**：
1. **瓶颈分析**：系统瓶颈在哪？是计算、访存还是通信？
2. **方案选择**：有哪些可选方案？各自的 trade-off 是什么？
3. **部署细节**：具体怎么实现？有哪些坑？
4. **监控回滚**：怎么保证上线后可监控、可回滚？

### 4.3 具体问题示例

#### Q1: 一个 LLM 模型怎么部署上线？

**回答**：

> **瓶颈分析**：
> - 7B 模型 FP16 需要 14GB 显存
> - 推理是 memory-bound（batch size 小时）
> - 长序列推理需要大量 KV cache
>
> **方案选择**：
>
> | 方案 | 优点 | 缺点 | 适用场景 |
> |------|------|------|---------|
> | 原始 PyTorch | 简单 | 慢，显存浪费 | 原型验证 |
> | vLLM | 高吞吐，PagedAttention | 需要 GPU | 生产环境 |
> | TensorRT-LLM | 最快 | 部署复杂 | 高性能要求 |
> | GGUF + llama.cpp | CPU/GPU 混合 | 速度较慢 | 本地部署 |
>
> **部署细节**：
> ```
> 1. 模型准备
> → 下载预训练权重
> → 可选：LoRA 微调 → 合并权重
> → 可选：量化（AWQ/GPTQ）→ 减少显存
>
> 2. 服务搭建
> → 选择推理框架（vLLM/TensorRT-LLM）
> → 配置参数：
>   - max_model_len: 最大序列长度
>   - gpu_memory_utilization: GPU 显存使用率
>   - max_num_seqs: 最大并发请求数
>
> 3. 部署方式
> → Docker 容器化
> → Kubernetes 编排
> → 负载均衡 + 自动扩缩容
> ```
>
> **监控回滚**：
> ```
> 监控指标：
> - QPS（每秒请求数）
> - P50/P99 延迟
> - GPU 显存/计算利用率
> - 错误率
> - 输出质量（采样人工评估）
>
> 回滚策略：
> - 保留旧版本镜像
> - 蓝绿部署或金丝雀发布
> - 监控异常时自动切回旧版本
> ```

#### Q2: 怎么保证 LLM 服务的稳定性？

**回答**：

> **风险点**：
> 1. **显存溢出**：长序列请求导致 OOM
> 2. **延迟飙升**：请求排队导致超时
> 3. **输出质量下降**：模型退化或数据漂移
>
> **稳定性措施**：
>
> ```
> 1. 输入限制
> → 限制最大输入长度（如 4096 tokens）
> → 限制 batch size
> → 请求超时设置
>
> 2. 资源管理
> → PagedAttention 管理 KV cache
> → 请求优先级队列
> → 动态 batch 策略
>
> 3. 降级策略
> → 高负载时自动降级到小模型
> → 长序列截断或摘要
> → 非核心请求拒绝
>
> 4. 监控告警
> → 显存使用率 > 90% 告警
> → P99 延迟 > 1s 告警
> → 错误率 > 1% 告警
> ```

#### Q3: SGEMM 优化的工程落地考虑

**回答**（基于项目经验）：

> **理论 vs 实践的差距**：
>
> ```
> 理论最优 vs 实际可用
>
> Kernel 7 (双缓冲) 达到 cuBLAS 的 99.7%
> 但：
> - 只在特定矩阵大小下最优
> - 不同 GPU 架构需要不同的参数调优
> - 编译器优化可能影响最终性能
>
> 工程落地考虑：
> 1. 可维护性：代码是否易于理解和修改
> 2. 可移植性：是否支持不同 GPU 架构
> 3. 健壮性：边界条件处理
> 4. 测试覆盖：是否有充分的测试用例
> ```
>
> **实际做法**：
> - 优先使用 cuBLAS/cuDNN（官方优化，稳定可靠）
> - 只在性能瓶颈处手写 kernel
> - 用 Triton 做原型验证，再用 CUDA 优化

---

## 五、业务与实际场景的理解

### 5.1 面试官的考察逻辑

```
"这个方案适合什么场景？"
    → 考察你是否理解技术的适用边界

"上线成本有多高？"
    → 考察你是否有成本意识

"如果资源有限，先优化什么？"
    → 考察你的优先级判断能力
```

### 5.2 回答框架：场景→成本→收益→优先级

**万能结构**：
1. **场景**：这个方案解决什么场景的问题？用户是谁？
2. **成本**：需要多少计算资源？开发时间？维护成本？
3. **收益**：能带来什么价值？性能提升多少？成本节省多少？
4. **优先级**：如果资源有限，应该先做什么？

### 5.3 具体问题示例

#### Q1: RAG 系统适合什么场景？不适合什么场景？

**回答**：

> **适合场景**：
> - **知识密集型任务**：需要查询大量文档、知识库
> - **时效性要求高**：知识需要频繁更新
> - **可解释性要求**：需要展示引用来源
> - **成本敏感**：不想为所有知识做微调
>
> **不适合场景**：
> - **推理密集型任务**：需要复杂逻辑推理，RAG 提供的上下文可能不够
> - **风格迁移任务**：需要改变模型的说话风格，RAG 无法解决
> - **实时性要求极高**：检索增加延迟，不适合毫秒级响应
> - **隐私敏感**：知识库可能包含敏感信息
>
> **用户关心什么**：
> ```
> 1. 回答准确性（最重要）
> 2. 响应速度（< 3s）
> 3. 引用来源（可解释性）
> 4. 成本（token 消耗）
> ```

#### Q2: 量化模型的上线成本和收益分析

**回答**：

> **成本分析**：
>
> | 成本项 | 详情 |
> |--------|------|
> | 量化成本 | 需要校准数据，GPTQ 量化 7B 模型约 2-4 小时 |
> | 精度损失 | INT4 量化可能损失 1-3% accuracy |
> | 部署复杂度 | 需要特定推理框架（如 AutoGPTQ） |
> | 维护成本 | 需要监控量化对不同任务的影响 |
>
> **收益分析**：
>
> | 收益项 | 详情 |
> |--------|------|
> | 显存节省 | INT4 量化：14GB → 3.5GB，节省 75% |
> | 推理加速 | Tensor Core INT8 吞吐量是 FP16 的 2 倍 |
> | 成本节省 | 可以用更小的 GPU，或服务更多并发 |
>
> **优先级判断**：
> ```
> 如果资源有限，优先级排序：
>
> 1. 先评估精度损失是否可接受
>   → 在目标任务上测试量化前后 accuracy
>   → 如果损失 < 1%，继续
>   → 如果损失 > 3%，考虑其他方案
>
> 2. 选择量化方法
>   → 对精度要求高：AWQ（保护重要权重）
>   → 对速度要求高：GPTQ（量化速度快）
>   → 需要 CPU 推理：GGUF
>
> 3. 逐步上线
>   → A/B 测试，对比量化前后效果
>   → 监控线上指标
>   → 准备回滚方案
> ```

#### Q3: 如果只能优化一个点，应该优化什么？

**回答**：

> **分析框架**：
> ```
> 优化优先级 = 收益 / 成本
>
> 收益 = 性能提升 × 用户价值
> 成本 = 开发时间 × 维护成本
> ```
>
> **LLM 推理场景**：
>
> | 优化点 | 收益 | 成本 | 优先级 |
> |--------|------|------|--------|
> | 量化 | 显存减少 75%，速度提升 2x | 低（工具成熟） | ⭐⭐⭐⭐⭐ |
> | 连续批处理 | 吞吐量提升 3-5x | 中（需要框架支持） | ⭐⭐⭐⭐ |
> | PagedAttention | 显存利用率提升 2x | 中（需要 vLLM） | ⭐⭐⭐⭐ |
> | FlashAttention | 速度提升 2x，显存减少 | 低（框架内置） | ⭐⭐⭐⭐⭐ |
> | 自定义 CUDA kernel | 速度提升 10-50% | 高（需要 CUDA 专家） | ⭐⭐ |
>
> **结论**：
> - 如果只能优化一个点：**量化**（收益高，成本低）
> - 如果可以优化两个点：**量化 + 连续批处理**
> - 自定义 CUDA kernel 是最后的选择（收益递减，成本高）

**项目关联**：
```
SGEMM 优化的优先级（项目中的渐进式优化）：

1. Shared memory tiling（收益最大，最容易实现）
2. Thread tile（中等收益，中等难度）
3. 双缓冲（收益递减，难度较高）

和 LLM 优化的优先级完全一致：
先做收益大的简单优化，再做收益小的复杂优化
```

---

## 六、综合面试场景模拟

### 场景 1：项目深挖

**面试官**："你这个 CUDA 项目，具体做了什么？遇到什么问题？怎么解决的？"

**回答**：

> **做了什么**：
> 实现了 SGEMM 的 7 个优化版本，从 naive 到接近 cuBLAS 性能。
>
> **遇到的问题**：
>
> ```
> 问题 1：Kernel 2 (Shared Memory Tiling) 性能提升不明显
> → 现象：只比 naive 快 1.5x
> → 排查：用 ncu profile，发现 shared memory bank conflict
> → 解决：对 shared memory 做 padding
> → 结果：性能提升到 3x
>
> 问题 2：Kernel 7 (双缓冲) 的正确性验证
> → 现象：输出结果和 cuBLAS 有微小误差
> → 排查：逐步检查每一步的中间结果
> → 发现：__syncthreads() 的位置不对
> → 解决：调整同步点位置
> → 结果：误差 < 1e-6
> ```
>
> **学到的教训**：
> 1. 优化前先 profile，找到真正的瓶颈
> 2. 每一步优化都要验证正确性
> 3. 渐进式优化，不要一次改太多

### 场景 2：LLM 算法问题

**面试官**："如果让你优化一个 LLM 的推理速度，你会怎么做？"

**回答**：

> **第一步：分析瓶颈**
> ```
> 用 nsys/ncu profile，看时间花在哪里：
> - 如果是 Attention：考虑 FlashAttention
> - 如果是 Linear：考虑量化
> - 如果是访存：考虑 kernel fusion
> ```
>
> **第二步：选择优化方案**
> ```
> 按优先级排序：
> 1. 量化（收益高，成本低）
> 2. FlashAttention（框架内置，开箱即用）
> 3. 连续批处理（提高吞吐量）
> 4. 自定义 kernel（最后手段）
> ```
>
> **第三步：验证效果**
> ```
> - 在目标数据集上测试 latency 和 throughput
> - 验证输出质量没有下降
> - A/B 测试线上效果
> ```

### 场景 3：问题定位

**面试官**："模型上线后，用户反馈回答质量下降，你怎么排查？"

**回答**：

> **Step 1: 确认问题**
> ```
> - 收集用户反馈的具体 case
> - 对比新旧模型的输出
> - 量化"质量下降"：准确率？相关性？幻觉率？
> ```
>
> **Step 2: 排查原因**
> ```
> 假设 1: 模型版本问题
> → 检查部署的 checkpoint 是否正确
> → 排除
>
> 假设 2: 数据分布漂移
> → 分析线上 query 的分布变化
> → 发现：某类 query 占比增加
>
> 假设 3: Prompt 变化
> → 检查 system prompt 是否被修改
> → 排除
>
> 假设 4: 上下文长度问题
> → 检查长序列请求的表现
> → 发现：长序列下幻觉率增加
> ```
>
> **Step 3: 定位根因**
> ```
> 长序列请求增加 → KV cache 不足 → attention 计算被截断
> → 模型无法正确关注前文 → 输出质量下降
> ```
>
> **Step 4: 修复**
> ```
> 短期：限制输入长度
> 中期：启用 PagedAttention
> 长期：使用长序列优化的模型
> ```

---

## 七、总结：展示深度的关键

### 7.1 面试官想听到的

```
❌ "我用了 FlashAttention，因为它快"
✅ "FlashAttention 解决了标准 Attention 的 O(N²) 显存问题，
    核心思想是 tiling + online softmax，
    类似我 SGEMM 项目中的 shared memory tiling 策略"
```

### 7.2 展示深度的技巧

```
1. 讲清楚"为什么"而非只讲"是什么"
   → 为什么需要这个方法？解决什么问题？

2. 讲清楚"局限性"
   → 什么场景下会失效？有什么 trade-off？

3. 讲清楚"实验细节"
   → 怎么验证的？指标是什么？结果如何？

4. 讲清楚"遇到的问题"
   → 遇到什么坑？怎么排查的？怎么解决的？

5. 讲清楚"业务价值"
   → 这个方案适合什么场景？成本收益如何？
```

### 7.3 CUDA 知识 → 面试深度

| CUDA 知识 | 面试中的深度展示 |
|-----------|-----------------|
| Shared Memory Tiling | "FlashAttention 的 tiling 策略和 SGEMM 完全一致" |
| Bank Conflict | "Attention 计算中也有 bank conflict，需要优化" |
| 双缓冲 | "流水线并行的硬件基础，通信-计算重叠的原理" |
| 计算访存比 | "LLM 推理是 memory-bound，所以量化比优化计算更有效" |
| Warp Shuffle | "Softmax/LayerNorm 的高效实现依赖 warp 级归约" |

---

## 附录：面试前 Checklist

```
□ 能否用 1 分钟讲清楚项目的核心贡献？
□ 能否讲清楚每个优化解决什么问题？
□ 能否讲清楚遇到的问题和解决方案？
□ 能否讲清楚实验设计和结果分析？
□ 能否讲清楚业务价值和成本收益？
□ 能否将 CUDA 知识和 LLM 算法联系起来？
□ 能否回答"为什么不用其他方案"的追问？
□ 能否回答"这个方案有什么局限性"的追问？
```
